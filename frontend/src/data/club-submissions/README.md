Place generated club submission JSON files here.

The Start a club workflow currently generates a JSON file in the browser and stores a queued copy in localStorage (`club-submission-queue`) for future integration.
